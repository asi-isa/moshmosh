import React from "react";
import { StyleSheet, Text, View } from "react-native";

const options = ["some", "options", "to have"];

export default function AppPicker() {
  return (
    <View>
      <Text></Text>
    </View>
  );
}

const styles = StyleSheet.create({});
